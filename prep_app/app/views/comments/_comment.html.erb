<li>
  <ul>
    <li><%= image_tag comment.user.try(:image) %></li>
    <li><%= comment.title %></li>
    <li><%= comment.comment %></li>
    <li><%= link_to 'Delete', comment_path(comment, commentable_type: commentable.class.to_s.underscore, commentable_id: commentable.id), method: :delete, data: { confirm: 'Are you sure?' } %></li>
  </ul>
</li> 